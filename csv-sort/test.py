import algorithm

if __name__ == '__main__' :
    algorithm.solution(
                        './tmp/iris.csv', 
                        './tmp/iris_sorted.csv', 
                        ['species'],
                        False
                        )